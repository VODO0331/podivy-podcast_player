name: my_audio_player
description: Audio player.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get: 
  just_audio: ^0.9.36
  just_audio_background: ^0.0.1-beta.11
  provider: ^6.1.2
  audio_service: ^0.18.12

  search_service:
    path: ../graphql_service_repository/search_service_fp
  
  firestore_service_repository:
    path: ../firestore_service_repository
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true