name: firestore_service_repository
description:  package which manages the firestore.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get: 
  cloud_firestore: ^4.17.0
  flutter_image_compress: ^2.1.0
  
  authentication_repository:
      path: ../authentication_repository


  my_audio_player:
      path: ../audio_player
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
